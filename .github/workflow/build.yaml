name: Build and Push Docker image to Docker Hub

on: push

jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Set simple SHA
        id: vars
        run: echo "SHA=$(echo ${GITHUB_SHA} | cut -c1-7)" >> $GITHUB_ENV

      - name: Build and Push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/simple_api_docker_cicd:latest
            ${{ secrets.DOCKER_HUB_USERNAME }}/simple_api_docker_cicd:${{ env.SHA }}

  # This is Deploying part of after pushing my image to docker hub.after push deploy this an aws ec2 via ssh
  # deploy_to_server:
  #   name: Deploy to Remote Server via SSH
  #   runs-on: ubuntu-latest
  #   needs: push_to_registry
  #   steps:
  #     - name: Checkout (optional)
  #       uses: actions/checkout@v3

  #     - name: Wait for registry to update (optional)
  #       run: sleep 5

  #     - name: Deploy via SSH - pull image & restart container
  #       uses: appleboy/ssh-action@v0.1.9
  #       with:
  #         host: ${{ secrets.SSH_HOST }}           
  #         username: ${{ secrets.SSH_USER }}       
  #         key: ${{ secrets.SSH_PRIVATE_KEY }}     
  #         port: ${{ secrets.SSH_PORT || 22 }}
  #         timeout: 600
  #         script: |
  #           set -e
  #           # optional: docker login if your image is private on Docker Hub
  #           echo "${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}" | docker login --username "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin
  #           # pull the new image (using the short sha tag from your earlier step)
  #           docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/simple_api_docker_cicd:${{ env.SHA }}
  #           # stop old container (ignore error if not running)
  #           docker stop simple_api || true
  #           docker rm simple_api || true
  #           # run new container
  #           docker run -d \
  #             --name simple_api \
  #             --restart unless-stopped \
  #             -p 3000:3000 \
  #             ${{ secrets.DOCKER_HUB_USERNAME }}/simple_api_docker_cicd:${{ env.SHA }}
